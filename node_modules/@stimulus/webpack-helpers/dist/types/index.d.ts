import { Application } from "@stimulus/core";
export interface ContextModule {
    (key: string): Module;
    keys(): Array<string>;
    resolve(key: string): number;
    id: number;
}
export interface Module {
    __esModule: boolean;
    default?: object;
}
export declare function autoload(contextModule: ContextModule, application: Application): void;
